# RttM  /  Regression toward the Mean

## How to update blog

    $ npm run build
    $ git add .
    $ git commit -m "commit message"
    $ git push origin master
    $ git subtree push --prefix build/ origin gh-pages
